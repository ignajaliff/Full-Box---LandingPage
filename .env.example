# Supabase — cliente (públicas, viajan al navegador)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — secreto (SOLO server, jamás con prefijo NEXT_PUBLIC_)
# Se usa recién cuando haya operaciones con service key (auth/admin).
SUPABASE_SERVICE_ROLE_KEY=
